<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Cart_view"></activity>
        <activity android:name=".Details_movie" />
        <activity android:name=".Transactions" />
        <activity android:name=".screen_movie" />
        <activity
            android:name=".screen_mov"
            android:label="@string/title_activity_screen_mov"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".movieWithSearch"
            android:label="@string/title_activity_movie_with_search"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".admin" />
        <activity android:name=".welcome_admin_animation" />
        <activity android:name=".movie_card" />
        <activity android:name=".mainScreen" />
        <activity android:name=".Investor_animated" />
        <activity android:name=".Investor" />
        <activity android:name=".Party_popper" />
        <activity android:name=".Login" />
        <activity android:name=".SignUp" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>